function test(string) {

    let stack = [];
    let tokens = {'(':false,'[':false,'{':false,')':true,']':true,'}':true};

    console.log(tokens['[']);

    for (let index in string) {
        let char = string.charAt(index);
        if(tokens.get(c)){

        }
    }

}

test("[{}]");